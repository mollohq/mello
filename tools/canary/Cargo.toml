[package]
name = "canary"
version = "0.0.0-DEV"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
mello-core = { path = "../../mello-core" }
tokio = { workspace = true, features = ["rt", "macros", "time"] }
env_logger = "0.11"
log = { workspace = true }
rand = "0.8"
