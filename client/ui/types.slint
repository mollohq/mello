export struct CrewData {
    id: string,
    name: string,
    member-count: int,
}

export struct MemberData {
    id: string,
    name: string,
    online: bool,
}

export struct ChatMessageData {
    sender-name: string,
    text: string,
    timestamp: string,
}
